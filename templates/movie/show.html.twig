{% extends 'base.html.twig' %}

{% block title %}Movie{% endblock %}

{% block body %}
    <h1>Movie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movie.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ movie.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_movie_index') }}" class="bg-blue-500 text-white font-bold px-4 py-2">back to list</a>

    <a href="{{ path('app_movie_edit', {'id': movie.id}) }}" class="bg-orange-500 text-white font-bold px-4 py-2">edit</a>

    {{ include('movie/_delete_form.html.twig') }}
{% endblock %}
